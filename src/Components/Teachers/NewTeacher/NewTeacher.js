import { Container } from '@mui/material';
import React from 'react';

const NewTeacher = () => {
    return (
        <div>
            <Container sx={{py: 10, textAlign:"center"}}>
                <h1>New Teachers Comming Soon......</h1>
            </Container>
        </div>
    );
};

export default NewTeacher;